import React from "react";

interface IF {}

const Footer: React.FC<IF> = (props: IF) => {
  return (
    <footer className="sr-only">
      <div>4D REPLAY</div>
    </footer>
  );
};
export default Footer;
